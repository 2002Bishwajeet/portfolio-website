export interface Service {
  icon: string;
  title: string;
  description: string;
}

export interface Skill {
  icon: string;
  title: string;
}

export interface Experience {
  title: string;
  company: string;
  date_joining: string;
  date_left: string;
  description: string;
}
